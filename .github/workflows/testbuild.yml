name: Test-Build
on:
  push:
    branches:
      - 4_17
jobs:
  for-hosted:
    strategy:
      fail-fast: false
      matrix:
        os:
        - 'macos-13'
        - 'macos-14'
        - 'macos-15'
        - 'ubuntu-22.04'
        - 'ubuntu-24.04'
        - 'ubuntu-24.04-arm'
    runs-on: ${{ matrix.os }}
    steps:
      - run: echo "event=${{ github.event_name }} runner=${{ runner.os }} os=${{ matrix.os }} branch=${{ github.ref }} repo=${{ github.repository }}."
      - uses: actions/checkout@v4
      - run: SUDO=sudo ./testbuild.sh --preinstall
      - run: echo "status=${{ job.status }}."
      - uses: actions/upload-artifact@v4
        with:
          name: DIST-${{ matrix.os }}-${{ strategy.job-index }}
          path: DIST/*
  for-containers:
    strategy:
      fail-fast: false
      matrix:
        os:
        - 'debian:8'
        - 'debian:9'
        - 'debian:11'
        - 'debian:testing'
        - 'alpine:3.9'
        - 'alpine:latest'
        - 'centos:7'
        - 'rockylinux:9'
        - 'fedora:30'
        - 'fedora:latest'
        - 'archlinux:latest'
        - 'gentoo/stage3:latest'
    runs-on: ubuntu-latest
    env:
      CONTAINER: ${{ matrix.os }}
    steps:
      - run: echo "event=${{ github.event_name }} runner=${{ runner.os }} os=${{ matrix.container }} branch=${{ github.ref }} repo=${{ github.repository }}."
      - uses: actions/checkout@v4
      - run: docker pull $CONTAINER
      - run: docker run --name container-"${CONTAINER//:/__}" -v ${{ github.workspace }}:/workspace $CONTAINER ./testbuild.sh --preinstall
      - run: echo "status=${{ job.status }}."
      - uses: actions/upload-artifact@v4
        with:
          name: DIST-${{ matrix.os }}-${{ strategy.job-index }}
          path: DIST
